{
  "name": "vitepress-demo",
  "type": "module",
  "scripts": {
    "dev": "rm -rf .cache && vitepress dev docs",
    "build": "rm -rf dist && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0",
    "vitepress": "^1.5.0"
  }
}
